{
	"name": "scelteapp-typescript-go",
	"version": "0.1.0",
	"license": "MIT",
	"private": true,
	"scripts": {
		"setup": "npm install && npm prune",
		"setup-all": "npm run setup && echo \"Download Golang dependencies...\" && go get -u ./...",
		"upgrade": "npx npm-check-updates -u && npm run setup",
		"upgrade-all": "npx npm-check-updates -u && npm run setup-all",
		"dev": "vite dev --open",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"lint": "prettier --check . && eslint .",
		"format": "prettier --write .",
		"build": "npx vite build",
		"build-all": "npm run build && echo \"Building Golang binary...\" && go build .",
		"serve": "open-cli http://localhost:8080 && main",
		"docker": "docker build . -t svelte-ts-go -f Dockerfile && open-cli http://localhost:8080 && docker run -p 8080:8080 --rm svelte-ts-go",
		"commit": "npm run format && git pull origin master && git add . && git commit -m \"Updating\" && git push origin master"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^1.0.5",
		"@sveltejs/kit": "^1.3.1",
		"@typescript-eslint/eslint-plugin": "^5.49.0",
		"@typescript-eslint/parser": "^5.49.0",
		"eslint": "^8.32.0",
		"eslint-config-prettier": "^8.6.0",
		"eslint-plugin-svelte3": "^4.0.0",
		"npm-check-updates": "^16.6.3",
		"open-cli": "^7.1.0",
		"prettier": "^2.8.3",
		"prettier-plugin-svelte": "^2.9.0",
		"svelte": "^3.55.1",
		"svelte-check": "^3.0.3",
		"svelte-preprocess": "^5.0.1",
		"tslib": "^2.5.0",
		"typescript": "^4.9.4",
		"vite": "^4.0.4"
	},
	"type": "module",
	"dependencies": {
		"axios": "^1.2.5",
		"bootstrap": "^5.2.3"
	}
}
